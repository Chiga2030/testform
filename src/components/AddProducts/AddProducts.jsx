import styles from './AddProducts.module.css';

const AddProducts = () => (
  <>
    <button
      type="button"
      className={styles.button}
    >
      Add more products
    </button>
  </>
);

export default AddProducts;
